# Listar drivers instalados

Audita drivers do sistema para diagnóstico e manutenção.

## Listar drivers
```cmd
pnputil /enum-drivers
```

## Listar dispositivos e status
```cmd
pnputil /enum-devices /connected
```